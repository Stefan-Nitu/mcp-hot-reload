{
  "name": "mcp-hot-reload",
  "version": "1.0.0",
  "description": "Hot-reload development tool for MCP (Model Context Protocol) servers with session preservation",
  "main": "dist/mcp-hot-reload.js",
  "type": "module",
  "bin": {
    "mcp-hot-reload": "dist/mcp-hot-reload.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "development",
    "hot-reload",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
