{
  "name": "mcp-dev-proxy",
  "version": "0.2.0",
  "description": "Robust hot-reload development proxy for MCP servers with session management",
  "main": "dist/mcp-dev-proxy.js",
  "type": "module",
  "bin": {
    "mcp-dev-proxy": "dist/mcp-dev-proxy.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "development",
    "hot-reload",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
